# Lab notebook: frozen baseline

Date: 2026-09-13 UTC
Branch: `research/next-optimizations`
Purpose: freeze the known-good state before the six follow-up research tracks.

## Hardware state

- 2× CMP 170HX, 65,536 MiB each
- PCIe Gen2 x4 per card
- same PCIe bridge, no NVLink/P2P
- `HW Power Brake Slowdown: Not Active`
- approximately 61.4 GiB allocated on each card while serving

Hardware identifiers are intentionally omitted from public artifacts.

## Software and model

- model: `Soomin33/Qwen3.8-Flash-Next-FP6-INT8`
- model revision: `42f2700ed675bab6176995e0c6a839240418bfba`
- SGLang: `0.0.0.dev1+g593134d17`
- base image digest: `sha256:5ae5816783d58e2e56e84d2e863f5441425056f500b7fbd7448c4aae017a2521`
- PyTorch: `2.13.0+cu130`
- Triton: `3.7.1`
- CUDA: `13.0`

## Frozen runtime

```text
TP=2
context=262144
mem_fraction_static=0.93
chunked_prefill_size=4096
max_running_requests=3
max_mamba_cache_size=24
mamba_ssm_dtype=float16
NEXTN steps=1 topk=1 draft_tokens=2
decode CUDA Graph batch=1,2,3
prefill CUDA Graph=disabled
RadixCache=enabled
overlap scheduler=enabled (default)
NCCL P2P=disabled
NCCL SHM=enabled
```

## Baseline performance

The latest matrix used three 512-token runs per concurrency, unique prompts, non-thinking mode,
temperature zero, and `ignore_eos=true`.

| concurrency | aggregate decode median | E2E output median | mean TTFT |
|---:|---:|---:|---:|
| 1 | 64.25 tok/s | 60.09 tok/s | 0.590 s |
| 2 | 99.02 tok/s | 93.74 tok/s | 0.608 s |
| 3 | 119.18 tok/s | 110.08 tok/s | 1.111 s |

Prefill and cache:

| input | TTFT / effective throughput |
|---:|---:|
| 1,268 tokens | 1.592 s / 796.70 tok/s |
| 4,985 tokens | 4.930 s / 1,011.08 tok/s |
| 19,832 tokens | 17.688 s / 1,121.19 tok/s |
| 9,943-token cold prefix | 9.201 s |
| same prefix, RadixCache hit | 0.612 s |

## Memory baseline

- GDN SSM state per GPU: 0.66 GB
- intermediate GDN SSM state per GPU: 0.21 GB
- KV capacity: 481,344 tokens
- memory-pool headroom after graph capture: approximately 4.8 GB per GPU

## Known acceptance behavior

The current configuration proposes one draft token plus the target bonus token. Code-generation
runs frequently showed acceptance rates from 0.90 to 1.00, but general and long-context prompts
also produced 0.4–0.8. The next experiment records counter deltas by workload; the latest gauge
alone is not a valid average.

## Baseline health gate

- `/health`: HTTP 200
- `/v1/models`: HTTP 200
- OpenWebUI: HTTP 200
- 16K and 64K needle probes at 10/50/90% depth: passed
- no current Power Brake, OOM, NaN, or stopped TP rank
